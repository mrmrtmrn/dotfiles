# 使い方ガイド

Neovim と Ghostty のカスタマイズ内容と操作方法のリファレンス。

---

## Neovim

### 基本設定

| 設定 | 値 |
|------|-----|
| Leader キー | `Space` |
| インデント | スペース2つ |
| クリップボード | システムと連携（`y` でコピー、`p` で貼り付け） |
| マウス | 有効 |

### モード切替・移動

| キー | 動作 | モード |
|------|------|--------|
| `jj` | ESC + 下に移動 + IME英語切替 | Insert |
| `kk` | ESC + 上に移動 + IME英語切替 | Insert |
| `H` | 前の単語へ（`b`） | Normal/Visual |
| `L` | 次の単語へ（`w`） | Normal/Visual |
| `J` | 次の空行へ（`}`） | Normal/Visual |
| `K` | 前の空行へ（`{`） | Normal/Visual |

### ファイル操作 (Telescope)

| キー | 動作 |
|------|------|
| `<Space>ff` | ファイル検索 |
| `<Space>fg` | grep検索（ファイル内容を検索） |
| `<Space>fb` | 開いているバッファ一覧 |

Telescope 内の操作:
- `Ctrl+j` / `Ctrl+k`: 上下移動
- `Enter`: 選択して開く
- `Ctrl+x`: 水平分割で開く
- `Ctrl+v`: 垂直分割で開く
- `Esc`: 閉じる

### ファイルツリー (nvim-tree)

| キー | 動作 |
|------|------|
| `<Space>e` | ファイルツリーにフォーカス |
| `q` | ファイルツリーを閉じる（ツリー内で） |

ツリー内の操作:
- `Enter`: ファイルを開く / フォルダを展開
- `a`: 新規ファイル作成
- `d`: 削除
- `r`: リネーム
- `c`: コピー
- `p`: 貼り付け
- `R`: 更新

### Git (LazyGit)

| キー | 動作 |
|------|------|
| `<Space>gg` | LazyGit を開く |

LazyGit 内は専用のキーバインド（`?` でヘルプ表示）

### LSP（コード補完・定義ジャンプ）

| キー | 動作 |
|------|------|
| `gd` | 定義へジャンプ |
| `gy` | 型定義へジャンプ |
| `gr` | 参照一覧 |
| `gK` | ホバー情報（ドキュメント表示） |
| `<Space>lr` | リネーム |
| `<Space>la` | コードアクション |
| `<Space>lf` | フォーマット |
| `<Space>ld` | 診断（エラー詳細）表示 |
| `]d` | 次のエラーへ |
| `[d` | 前のエラーへ |

対応言語: TypeScript/JavaScript, HTML, CSS, Tailwind CSS, JSON, Lua

**自動フォーマット**: ファイル保存時に自動実行

### 補完 (nvim-cmp)

| キー | 動作 |
|------|------|
| `Ctrl+Space` | 補完メニューを開く |
| `Tab` / `Ctrl+j` | 次の候補 |
| `Shift+Tab` / `Ctrl+k` | 前の候補 |
| `Enter` | 確定 |
| `Ctrl+e` | キャンセル |

補完ソース（優先順）: LSP → スニペット → バッファ内単語 → ファイルパス

### Markdown

| キー | 動作 |
|------|------|
| `<Space>mp` | ブラウザでプレビュー表示/非表示 |
| `Tab` | 箇条書きのインデントを深く（Insert） |
| `Shift+Tab` | 箇条書きのインデントを浅く（Insert） |

箇条書き（`-`, `*`, `1.` など）は Enter で自動継続

### ヘルプ (which-key)

`Space` を押して 300ms 待つとキーマップのヒントが表示される

---

## Ghostty

### 画面分割

| キー | 動作 |
|------|------|
| `Cmd+Enter` | 下に分割 |
| `Cmd+Shift+Enter` | 右に分割 |
| `Cmd+W` | 現在のペインを閉じる |

### 分割画面の移動

| キー | 動作 |
|------|------|
| `Cmd+H` | 左のペインへ |
| `Cmd+J` | 下のペインへ |
| `Cmd+K` | 上のペインへ |
| `Cmd+L` | 右のペインへ |

### 分割サイズの調整

| キー | 動作 |
|------|------|
| `Cmd+Shift+←` | 左に広げる |
| `Cmd+Shift+→` | 右に広げる |
| `Cmd+Shift+↑` | 上に広げる |
| `Cmd+Shift+↓` | 下に広げる |

### その他の設定

- **テーマ**: tokyonight
- **フォント**: PlemolJP Console NF 18pt
- **`¥` キー**: バックスラッシュ `\` を入力（日本語キーボード対策）
- **選択でコピー**: テキスト選択するだけでクリップボードにコピー
- **Option キー**: Alt として動作（Vim等のショートカット用）
- **ウィンドウ状態**: 終了時に保存、次回起動時に復元
